# Public Expo client configuration (never put secrets in EXPO_PUBLIC_* variables)
EXPO_PUBLIC_API_BASE_URL=https://api.example.invalid

# Server-only configuration; set this on the separately deployed API server
OPENROUTER_API_KEY=replace_with_server_only_openrouter_key
